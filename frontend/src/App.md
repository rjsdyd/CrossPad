# App

## 프로젝트의 최상위 컴포넌트 (상태 관리 및 레이아웃 정의)

- **전역 상태 관리**: `activeTab` 상태를 통해 현재 활성화된 화면(NINTENDO, PLAYSTATION, LOGIN, SIGNUP, MYPAGE 등)을 관리합니다.
- **유저 인증 상태**: `user` 상태를 통해 로그인된 사용자의 정보를 저장하고 관리합니다. (null일 경우 로그아웃 상태)
- **조건부 렌더링 (라우팅)**: `renderContent()` 함수를 사용하여 `activeTab` 상태에 따라 메인 피드(`MainFeed`) 또는 인증 관련 화면(`Login`, `Signup`, `MyPage`)을 동적으로 렌더링합니다.
- **공통 레이아웃 구성**: 데스크탑을 위한 `Sidebar`와 모바일을 위한 `BottomNav`를 배치하고 컨텐츠 영역을 분리합니다.

### 업데이트 내역
#### 2026-05-21
- **컴포넌트 경로 수정**: 구조 개편에 맞춰 `MainFeed`, `AdminDashboard` 컴포넌트의 import 경로 오류 해결

#### 2026-05-27
- **찜 목록 라우팅 연결**: 마이페이지에서 전달된 `BOOKMARKS` 상태를 감지하여 `BookmarkPage` 컴포넌트 화면을 렌더링하도록 조건부 라우팅 분기 추가

#### 2026-05-28
- **찜 목록(Bookmark) 페이지 개선**:
  - 플랫폼별(전체, 닌텐도 스위치, 플레이스테이션) 필터링 기능 추가 및 브랜드 컬러 배지 적용
  - 게임 카드 클릭 시 자체적으로 상세 페이지(`GameDetail`)를 띄우도록 이벤트 연결
  - 리스트 UI 규격 통일 (모든 카드 높이 150px 고정, 제목 및 배지 너비 고정 및 말줄임표 처리 적용)
- **사용자 경험(UX) 및 인터페이스 개선**:
  - 찜하기/취소 시 기존 투박한 `alert()` 대신 우측 하단 카드형 토스트(Toast) 알림 적용 (부드러운 애니메이션 및 3초 유지)
- **스크롤 위치 복원 기능 구현**:
  - `MainFeed`, `BookmarkPage`, `RankingPage`, `UpcomingPage`, `SearchPage` 화면에서 상세 페이지 열람 후 뒤로 가기 시, `sessionStorage`를 활용해 보고 있던 스크롤 위치로 자동 복귀
  - `GameDetail` 진입 시에는 항상 스크롤이 최상단으로 가도록 처리
- **기타 UI/레이아웃 수정**:
  - `MyPage` 찜 목록 보러가기 버튼 하트 아이콘 제거 및 중앙 정렬
  - `Sidebar` 하단의 이용약관/개인정보처리방침 부분 상단 구분선 제거 및 `marginTop: auto`를 활용해 메뉴 최하단으로 밀착 고정